---
title: 卷积神经网络
alias: Convolutional Neural Network, CNN
tags:
  - AI
  - 深度学习
  - 计算机视觉
category: 深度学习
created: 2026-03-31
updated: 2026-03-31
author: KnowGraph
description: CNN是一种专门用于处理图像的深度神经网络，通过卷积层自动学习空间层级特征。
mastery: 0
rating: 0
related_concepts:
  - 深度学习
  - 计算机视觉
  - 图像分类
difficulty: 中等
read_time: 10分钟
prerequisites:
  - 神经网络基础
---

# 卷积神经网络

## 一句话定义

> CNN是一种使用卷积运算自动学习图像空间层级特征的深度神经网络。

## 核心公式

### 2D 卷积

$$
Y_{i,j} = \sum_{m}\sum_{n} K_{m,n} \cdot X_{i+m, j+n} + b
$$

其中 $K$ 是卷积核（滤波器），$X$ 是输入图像。

## 详细说明

### 1. CNN 的核心思想

**局部连接 + 权重共享**

- 每个神经元只感受局部区域（感受野）
- 同一层的卷积核参数共享
- 大幅减少参数量

### 2. 典型结构

```
输入图像
  ↓
卷积层 (Conv) → 激活函数 (ReLU)
  ↓
池化层 (Pooling)
  ↓
卷积层 (Conv) → 激活函数 (ReLU)
  ↓
池化层 (Pooling)
  ↓
全连接层 (FC)
  ↓
输出 (分类/检测等)
```

### 3. 经典架构

| 网络 | 年份 | 贡献 |
|------|------|------|
| LeNet | 1998 | 早期CNN，手写数字识别 |
| AlexNet | 2012 | 深度学习复兴，ImageNet冠军 |
| VGG | 2014 | 深网络，3x3卷积堆叠 |
| ResNet | 2015 | 残差连接，解决梯度消失 |
| EfficientNet | 2019 | 复合缩放，效率与精度平衡 |

### 4. 代码实现

```python
class SimpleCNN(nn.Module):
    def __init__(self, num_classes=10):
        super().__init__()
        self.conv1 = nn.Conv2d(3, 32, kernel_size=3, padding=1)
        self.conv2 = nn.Conv2d(32, 64, kernel_size=3, padding=1)
        self.pool = nn.MaxPool2d(2, 2)
        self.fc1 = nn.Linear(64 * 8 * 8, 128)
        self.fc2 = nn.Linear(128, num_classes)

    def forward(self, x):
        x = self.pool(F.relu(self.conv1(x)))  # 32x16x16
        x = self.pool(F.relu(self.conv2(x)))  # 64x8x8
        x = x.view(-1, 64 * 8 * 8)
        x = F.relu(self.fc1(x))
        x = self.fc2(x)
        return x
```

## 应用场景

- 图像分类
- 目标检测
- 语义分割
- 人脸识别
- 自动驾驶

## 相关概念

- [[深度学习]] — CNN是深度学习的重要分支
- [[计算机视觉]] — CNN是CV领域的核心技术
- [[残差网络]] — ResNet解决了深层CNN的训练问题

## 延伸阅读

- [CS231n CNN](http://cs231n.github.io/convolutional-networks/)
- [AlexNet论文](https://papers.nips.cc/paper/4824-imagenet-classification-with-deep-convolutional-neural-networks)
