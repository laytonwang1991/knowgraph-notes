---
title: 残差网络
alias: Residual Network, ResNet
tags:
  - AI
  - 深度学习
  - 计算机视觉
category: 深度学习
created: 2026-03-31
updated: 2026-03-31
author: KnowGraph
description: ResNet是一种使用残差连接解决深层神经网络训练问题的卷积神经网络架构。
mastery: 0
rating: 0
related_concepts:
  - 深度学习
  - 卷积神经网络
  - 图像分类
difficulty: 中等
read_time: 8分钟
prerequisites:
  - 卷积神经网络
  - 神经网络基础
---

# 残差网络

## 一句话定义

> ResNet通过引入残差连接（skip connection）解决了深层网络难以训练的问题，使得训练数百层的网络成为可能。

## 核心公式

### 残差块

$$
y = \mathcal{F}(x, \{W_i\}) + x
$$

其中 $\mathcal{F}(x, \{W_i\})$ 是要学习的残差映射，$x$ 是输入的恒等映射。

## 详细说明

### 1. 为什么需要残差网络？

传统CNN的问题：
- 网络越深，训练误差反而增加
- 梯度消失/爆炸
- 退化问题（不是过拟合，是训练误差增加）

### 2. 残差连接原理

```
输入 x
  ↓
卷积层 → 卷积层
  ↓
恒等映射 x（捷径连接）
  ↓
输出：F(x) + x
```

### 3. ResNet 结构

| 层数 | 输出大小 | 结构 |
|------|----------|------|
| conv1 | 112x112 | 7x7, 64, stride 2 |
| conv2_x | 56x56 | 3x3 max pool, 64 |
| conv3_x | 28x28 | 3x3, 128, stride 2 |
| conv4_x | 14x14 | 3x3, 256, stride 2 |
| conv5_x | 7x7 | 3x3, 512, stride 2 |
| | 1x1 | avg pool, 1000-d fc |

### 4. 代码实现

```python
class ResidualBlock(nn.Module):
    def __init__(self, in_channels, out_channels, stride=1):
        super().__init__()
        self.conv1 = nn.Conv2d(in_channels, out_channels,
                                kernel_size=3, stride=stride, padding=1)
        self.bn1 = nn.BatchNorm2d(out_channels)
        self.conv2 = nn.Conv2d(out_channels, out_channels,
                                kernel_size=3, stride=1, padding=1)
        self.bn2 = nn.BatchNorm2d(out_channels)

        # 捷径连接
        self.shortcut = nn.Sequential()
        if stride != 1 or in_channels != out_channels:
            self.shortcut = nn.Sequential(
                nn.Conv2d(in_channels, out_channels,
                          kernel_size=1, stride=stride),
                nn.BatchNorm2d(out_channels)
            )

    def forward(self, x):
        out = F.relu(self.bn1(self.conv1(x)))
        out = self.bn2(self.conv2(out))
        out += self.shortcut(x)  # 残差连接
        out = F.relu(out)
        return out
```

## 相关概念

- [[卷积神经网络]] — ResNet是CNN的重要变体
- [[深度学习]] — ResNet解决了深度学习训练问题
- [[图像分类]] — ResNet在图像分类任务上取得了突破性成绩

## 延伸阅读

- [ResNet论文](https://arxiv.org/abs/1512.03385)
- [The Illustrated ResNet](https://t.ly/98_O)
