---
title: 降维
alias: Dimensionality Reduction
tags:
  - AI
  - 机器学习
  - 无监督学习
  - 算法
category: 机器学习
created: 2026-03-31
updated: 2026-03-31
author: KnowGraph
description: 降维是通过某种映射方法将高维数据转换为低维表示，同时尽可能保留原有数据关键信息的技术。
mastery: 0
rating: 0
related_concepts:
  - PCA
  - t-SNE
  - 线性判别分析
  - 无监督学习
  - 特征选择
difficulty: 进阶
read_time: 12分钟
prerequisites:
  - 监督学习
  - 线性代数
---

# 降维

## 一句话定义

> 降维是通过数学变换将高维数据映射到低维空间，在保留数据关键结构信息的同时去除冗余和噪声的技术。

## 核心公式

### PCA（主成分分析）

$$
\min_{W \in \mathbb{R}^{d \times k}} \| X - W W^T X \|_F^2
$$

其中 $W$ 是正交基矩阵，$k$ 是目标维度。

### 协方差矩阵

$$
\Sigma = \frac{1}{N} \sum_{i=1}^{N} (x_i - \mu)(x_i - \mu)^T
$$

### t-SNE 相似度

$$
P_{j|i} = \frac{\exp(-\| x_i - x_j \|^2 / 2\sigma_i^2)}{\sum_{k \neq i} \exp(-\| x_i - x_k \|^2 / 2\sigma_i^2)}
$$

$$
q_{ij} = \frac{(1 + \| y_i - y_j \|^2)^{-1}}{\sum_{k \neq l} (1 + \| y_k - y_l \|^2)^{-1}}
$$

### LDA（线性判别分析）

$$
J(w) = \frac{w^T S_B w}{w^T S_W w}
$$

其中 $S_B$ 是类间散度矩阵，$S_W$ 是类内散度矩阵。

## 详细说明

### 1. 降维方法分类

```
降维方法
    ├── 线性方法
    │   ├── PCA（主成分分析）
    │   ├── LDA（线性判别分析）
    │   └── SVD（奇异值分解）
    └── 非线性方法
        ├── t-SNE
        ├── UMAP
        ├── ISOMAP
        └── LLE（局部线性嵌入）
```

### 2. PCA 算法流程

```
1. 中心化：X = X - mean(X)
2. 计算协方差矩阵：Σ = (X^T X) / (N-1)
3. 特征值分解：Σ = V Λ V^T
4. 选择前k个特征向量组成投影矩阵 W
5. 降维结果：Y = X W
```

### 3. 常用降维方法对比

| 方法 | 类型 | 保留特性 | 计算复杂度 | 应用场景 |
|------|------|----------|------------|----------|
| PCA | 线性 | 方差 | O(nd²) | 通用降维、去噪 |
| t-SNE | 非线性 | 局部结构 | O(n²) | 可视化 |
| UMAP | 非线性 | 全局+局部 | O(n log n) | 可视化、分析 |
| LDA | 线性监督 | 类别可分性 | O(nd²) | 特征提取、分类 |

### 4. 降维目的

- **缓解维度灾难**：高维数据计算代价高
- **去噪**：去除不重要的特征
- **可视化**：将高维数据映射到2-3维
- **特征提取**：生成更具表示能力的新特征
- **压缩**：减少存储空间

### 5. 代码示例

```python
from sklearn.decomposition import PCA
from sklearn.manifold import TSNE
from sklearn.discriminant_analysis import LinearDiscriminantAnalysis
from sklearn.preprocessing import StandardScaler

# 数据标准化（PCA前建议标准化）
scaler = StandardScaler()
X_scaled = scaler.fit_transform(X)

# PCA降维
pca = PCA(n_components=2)
X_pca = pca.fit_transform(X_scaled)
print(f"解释方差比: {pca.explained_variance_ratio_}")

# t-SNE降维（常用于可视化）
tsne = TSNE(n_components=2, perplexity=30, random_state=42)
X_tsne = tsne.fit_transform(X_scaled)

# UMAP降维
# import umap
# reducer = umap.UMAP(n_components=2)
# X_umap = reducer.fit_transform(X_scaled)

# LDA降维（监督）
lda = LinearDiscriminantAnalysis(n_components=2)
X_lda = lda.fit_transform(X_scaled, y)
```

### 6. 如何选择降维方法

| 场景 | 推荐方法 |
|------|----------|
| 数据预处理/去噪 | PCA |
| 高维数据可视化 | t-SNE, UMAP |
| 分类前特征提取 | LDA |
| 保留全局结构 | UMAP, ISOMAP |
| 保留局部结构 | t-SNE, LLE |

## 相关概念

- [[PCA]] — 最常用的线性降维方法
- [[t-SNE]] — 非线性降维，常用于高维数据可视化
- [[线性判别分析]] — 监督降维方法
- [[无监督学习]] — 降维主要是无监督学习技术
- [[特征选择]] — 降维的另一种方法是特征选择

## 延伸阅读

- [Scikit-learn降维文档](https://scikit-learn.org/stable/modules/decomposition.html)
- [PCA详解](https://www.geeksforgeeks.org/principal-component-analysis-pca/)
- [t-SNE论文](https://www.jmlr.org/papers/v9/vandermaaten08a.html)
- [UMAP文档](https://umap-learn.readthedocs.io/)
- [降维技术综述](https://arxiv.org/abs/1404.1100)
